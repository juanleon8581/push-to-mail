<resources>
    <!-- Oscuro fijo, no sigue el modo del sistema. Solo afecta la ventana antes
         de que Compose tome control (fondo, status/nav bar); los colores reales
         de la UI los define PushToMailTheme (ui/theme/Theme.kt). -->
    <style name="Theme.PushToMail" parent="@android:style/Theme.Material.NoActionBar">
        <item name="android:colorBackground">#121212</item>
        <item name="android:statusBarColor">#121212</item>
        <item name="android:navigationBarColor">#121212</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:windowLightNavigationBar">false</item>
    </style>
</resources>
